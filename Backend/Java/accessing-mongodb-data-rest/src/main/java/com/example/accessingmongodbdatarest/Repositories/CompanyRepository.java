/*
package com.example.accessingmongodbdatarest.Repositories;

import com.example.accessingmongodbdatarest.Entities.Company;
import org.springframework.data.repository.CrudRepository;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

@Repository
public interface CompanyRepository extends CrudRepository<Company, Long> {

    public Company findByName(@Param("name") String name);

    public Company findById(@Param("id") long id);

    public Company findByUser_Id(long id);

    public Company findCompanyByProducts(long id);

    //List<Company> findAllBy();
}
*/
